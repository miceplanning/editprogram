# 드림투어 행사 안내 페이지

행사 당일 참가자에게 링크로 공유하는 모바일 중심 안내 페이지입니다.
Node.js나 별도 빌드 과정 없이 동작하는 순수 HTML/CSS/JS 정적 사이트라서,
파일만 있으면 바로 웹 호스팅(GitHub Pages, Vercel 등)에 올릴 수 있습니다.

---

## 🔥 다음 행사 준비할 때 (운영팀용 요약)

콘텐츠를 바꾸는 방법은 두 가지입니다. **편한 쪽으로 아무거나 쓰시면 됩니다.**

### 방법 A. 편집기 화면에서 수정 (추천 — 코드 몰라도 OK)

**여러 회차를 미리 준비해두고 싶다면 [projects.html](projects.html)(프로젝트 목록)에서
시작하세요.** "+ 새 프로젝트"를 누르면 빈 편집기가 열리고, 이미 만든 사이트가
있다면 "📥 현재 content.js 불러오기"로 첫 프로젝트를 시작할 수 있습니다. 여기서
만든 초안들은 지금 쓰는 브라우저에 자동으로 저장되어(다시 새로고침해도 안 사라짐),
언제든 목록에서 "열기"를 눌러 이어서 편집할 수 있습니다. (단, 이 저장은 "이
브라우저 안에서만" 유효합니다 — 다른 컴퓨터와 공유되지 않으니, 다음 행사가
확정되면 아래 3번처럼 꼭 `content.js`로 다운로드해서 실제 사이트에 반영해주세요)

당장 하나만 빠르게 고칠 거라면 목록 화면 없이 **[editor.html](editor.html)** 을
바로 열어도 됩니다 (더블클릭 또는 로컬 서버로 접속). 이 경우 지금 배포된
`content.js` 내용을 바로 불러와서 편집하고, 자동 저장 없이 예전처럼 동작합니다.

1. 위 두 가지 중 하나로 편집기를 엽니다.
2. 화면의 폼에 행사명, 날짜, 일정, 연락처 등을 입력/수정합니다.
   - 스태프·일정·장소·체크리스트·FAQ는 "+ 추가" 버튼으로 항목을 늘리고,
     각 카드의 ↑ ↓ 버튼으로 순서를 바꾸고, "삭제" 버튼으로 지울 수 있습니다.
   - 대표 사진 / 스태프 사진은 "🖼️ 사진 선택" 버튼으로 내 컴퓨터의
     사진 파일을 직접 골라 넣을 수 있습니다 (미리보기가 바로 보입니다).
     "행사 기본 정보"의 **"📁 images 폴더 연결하기"** 버튼을 한 번 눌러 프로젝트의
     `images` 폴더를 연결해두면, 그다음부터 사진을 고를 때마다 자동으로 그 폴더에
     저장됩니다. (Chrome/Edge에서 로컬 서버로 열었을 때만 지원 — 지원하지 않는
     브라우저거나 `editor.html`을 더블클릭해서 열었다면, 고른 사진을 직접
     `images` 폴더에 복사해주세요. 파일은 이미 내 컴퓨터에 있으니 복사만 하면 됩니다)
3. 맨 아래 **"⬇ content.js 파일 저장"** 버튼을 누르면 새 `content.js` 파일이
   다운로드됩니다.
4. 다운로드된 파일을 프로젝트의 **js/content.js 위치에 덮어쓰기**한 뒤,
   평소처럼 GitHub에 올리면(push) 실제 사이트에 반영됩니다.

> ⚠️ editor.html은 참가자에게 공유하는 페이지가 아니라 운영팀만 쓰는 도구입니다.
> 서버 없이 브라우저 안에서만 동작하며(값이 자동 저장되지 않음), 페이지를
> 새로고침/닫기 전에 꼭 "content.js 파일 저장" 버튼을 눌러야 합니다.
> 사이트를 배포하면 `editor.html`도 함께 올라가지만, 홈/하단 메뉴 어디에서도
> 링크되어 있지 않고 비밀번호로 보호되지도 않습니다. 주소를 아는 사람은
> 누구나 열어볼 수 있으니, 민감한 정보(개인정보 등)는 입력하지 마세요.

### 방법 B. js/content.js 파일을 직접 텍스트 편집기로 수정

> **[js/content.js](js/content.js)**

이 파일 안에 행사명, 날짜, 공지사항, 담당자 연락처, 일정표, 지도 링크, FAQ,
설문조사 링크 등 화면에 보이는 거의 모든 텍스트가 들어있습니다.
파일을 열어보면 각 항목 위에 한글 설명 주석이 달려있어서, 무엇을 바꾸면
되는지 바로 알 수 있습니다. (메모장, VS Code 등 아무 텍스트 편집기로 열면 됩니다)

두 방법 모두 사진은 **[images](images)** 폴더에 넣고, `content.js`에서 지정한
파일명과 동일하게 저장하면 됩니다. (자세한 파일명은 [images/README.txt](images/README.txt) 참고)

### 항목별로 어디를 고치면 되는지 (방법 B 기준)

| 바꾸고 싶은 내용 | 수정할 위치 |
| --- | --- |
| 행사명, 날짜, 대표 사진, 한 줄 소개 | `content.js` 상단 "1. 행사 기본 정보" |
| 이번 회차에 필요 없는 섹션 통째로 끄기 (우천안내/스태프/집결정보/체크리스트/FAQ/설문/항공권) | `content.js` "1-1. 섹션 사용 여부" `sectionsEnabled` (또는 편집기에서 각 패널의 "이 섹션 사용" 체크박스) |
| 공지사항 배너 문구 (끄고 싶으면 `active: false`) | `content.js` "2. 공지사항 배너" |
| 우천 시 대응 안내 문구 | `content.js` "3. 우천 시 대응 안내" |
| 당일 담당자 이름/전화번호 (헤더·플로팅 버튼) | `content.js` "4. 당일 담당자 연락처" |
| 현장 스태프 소개 카드 | `content.js` "4. 당일 담당자 연락처" 아래 `staff` 배열 |
| 집결 시간/장소/지도 링크 | `content.js` "5. 집결 정보" |
| 시간별 일정표 (타임테이블), 각 일정 관련 사진 | `content.js` "6. 시간별 일정" `schedule` 배열 (각 항목의 `photo` 필드) |
| 여러 날에 걸친 행사의 날짜 구분 (예: 1박 2일) | `schedule` 배열 각 항목의 `date` 필드 (예: `"2026-07-02"`, 편집기에서는 날짜 선택기로 입력) — 하루짜리 행사면 비워두면 됩니다. 하나라도 값이 있으면 일정 페이지가 날짜별로 자동으로 묶이고, 하단 "다음 일정" 안내도 오늘 날짜를 기준으로 정확히 찾아줍니다 |
| 오시는 길 페이지의 장소 목록/동선 순서 | `content.js` "7. 장소 목록" `locations` 배열 |
| 준비물 체크리스트 항목 | `content.js` "8. 준비물 체크리스트" |
| 자주 묻는 질문 | `content.js` "9. FAQ" |
| 설문조사 링크 / 예상 소요시간 | `content.js` "10. 설문조사 링크" |
| 항공권 정보 입력 구글폼 링크, 참가자별 항공편/예약번호 명단 | `content.js` "11. 항공권 정보" `flight` (또는 편집기 "항공권 정보" 패널에서 엑셀 표를 그대로 붙여넣기) |
| 사진 파일 | `images/` 폴더 |
| 전체 디자인/색상 (파란색 브랜드 컬러 등) | `css/style.css` 상단 `:root` 변수 (개발자용) |

**HTML(`index.html` 등)이나 `js/main.js`, `js/home.js` 같은 화면 구성 코드는
평소에는 건드릴 필요가 없습니다.** 구조 자체를 바꾸고 싶을 때만 손대면 됩니다.

---

## 📁 폴더 구조

```
.
├── index.html          # 홈 (행사 개요, 오늘 타임라인 요약)
├── schedule.html        # 일정(타임테이블) 페이지
├── upload.html            # 사진업로드 페이지 (구글폼/드라이브 연결)
├── flight.html               # 항공권 조회 페이지 (본인 성함으로 조회)
├── location.html            # 오시는 길 / 지도 페이지
├── faq.html                   # 자주 묻는 질문
├── survey.html                  # 설문조사 안내 페이지
├── editor.html                    # ⭐ 운영팀 전용 콘텐츠 편집기 (참가자 비공개)
├── projects.html                    # ⭐ 운영팀 전용 프로젝트(행사 초안) 목록
├── css/
│   ├── style.css        # 공통 디자인 (브랜드 컬러, 반응형 레이아웃)
│   ├── editor.css        # 편집기 전용 스타일
│   └── projects.css       # 프로젝트 목록 전용 스타일
├── js/
│   ├── content.js       # ⭐ 운영팀이 매 행사마다 수정하는 콘텐츠 데이터 파일
│   ├── photo-upload.js    # 구글폼/드라이브 링크 헬퍼 (사진 업로드 공용 로직)
│   ├── project-store.js     # 프로젝트 목록의 localStorage 저장/조회 헬퍼
│   ├── main.js                 # 공통 레이아웃 (공지배너/헤더/연락처버튼/하단메뉴)
│   ├── home.js                   # 홈 페이지 렌더링 로직
│   ├── schedule.js                 # 일정 페이지 렌더링 로직 (아코디언 타임라인)
│   ├── upload.js                     # 사진업로드 페이지 렌더링 로직
│   ├── flight.js                        # 항공권 조회 페이지 렌더링 로직 (성함 검색)
│   ├── location.js                     # 지도 페이지 렌더링 로직
│   ├── faq.js                            # FAQ 페이지 렌더링 로직
│   ├── survey.js                           # 설문조사 페이지 렌더링 로직
│   ├── editor.js                             # 편집기(editor.html) 동작 로직
│   └── projects.js                             # 프로젝트 목록(projects.html) 동작 로직
├── images/               # 행사 사진 (README.txt에 필요한 파일명 안내)
└── README.md
```

---

## 🖥️ 로컬에서 미리보기

빌드 과정이 필요 없습니다. `index.html` 파일을 더블클릭해서 브라우저로 열면
바로 확인할 수 있습니다. (인터넷 연결이 있어야 지도가 정상적으로 보입니다)

모바일 화면으로 보고 싶다면, 브라우저 개발자 도구(F12)의 기기 툴바를
켜서 확인하거나, 실제 스마트폰으로 파일을 전송해서 열어보세요.

---

## 🚀 배포 방법

### 방법 1. Vercel (추천 — 링크 발급이 가장 간단)

1. [vercel.com](https://vercel.com) 가입 후 이 프로젝트 폴더를 GitHub 저장소로 올립니다.
2. Vercel에서 "New Project" → 해당 저장소 선택 → Framework Preset은
   **Other**로 두고 그대로 배포(Deploy)합니다. (빌드 명령 없이 정적 파일 그대로 배포됩니다)
3. 배포가 끝나면 `https://프로젝트명.vercel.app` 같은 링크가 생성됩니다.
4. 다음 행사 때는 `content.js`와 사진만 교체하고 다시 GitHub에 푸시하면
   Vercel이 자동으로 재배포합니다.

### 방법 2. GitHub Pages (무료, 별도 서비스 가입 불필요)

1. 이 폴더를 GitHub 저장소에 푸시합니다.
2. 저장소의 **Settings → Pages** 메뉴에서 Source를 `main` 브랜치, 폴더는
   `/ (root)`로 설정합니다.
3. 잠시 후 `https://아이디.github.io/저장소명/` 링크로 접속할 수 있습니다.

---

## 📸 참가자 사진 업로드 기능 설정하기 (선택, 활동마다 한 번씩)

일정 중 특정 활동(예: 팀별 미션, 단체 활동)에서 **참가자가 직접 사진을 올리고,
운영팀/다른 참가자는 그 사진들을 모아볼 수 있는 기능**입니다. 이 사이트는 서버가
없는 정적 사이트라서, **구글폼(업로드 창구) + 구글 드라이브(저장 공간)** 를
그대로 이용합니다. 완전 무료이고, 신용카드 등록도 필요 없습니다.

설정 전에는 "사진 업로드 링크가 아직 준비되지 않았습니다" 문구만 표시되므로,
급하지 않다면 건너뛰어도 사이트의 다른 기능에는 전혀 영향이 없습니다.

### 1) 사진을 받을 구글폼 만들기

1. [Google Forms](https://forms.google.com)에서 **"새 양식"** 을 만듭니다.
2. 제목을 알아보기 쉽게 입력합니다. (예: "곶자왈 트레킹 사진 업로드")
3. 질문을 추가하고 유형을 **"파일 업로드"** 로 선택합니다.
   (팀명/이름을 받고 싶다면 "단답형" 질문을 하나 더 추가하세요)
4. 파일 업로드 질문을 처음 추가하면 "응답자가 파일을 업로드하도록 허용" 관련
   권한 확인 창이 뜹니다 → 허용해주세요. 최대 파일 개수·용량(예: 1개, 10MB)과
   허용 형식(이미지)을 설정할 수 있습니다.
5. 우측 상단 **"보내기"** → 링크 아이콘 클릭 → **"URL 단축"** 체크 → 링크 복사.
   (`https://forms.gle/...` 형태의 짧은 링크가 편합니다)

### 2) 업로드된 사진이 쌓이는 드라이브 폴더 링크 가져오기

파일 업로드 질문을 추가하면 구글이 자동으로 내 드라이브에
**"{설문지 제목} (파일 응답)"** 라는 폴더를 만들어 둡니다.

1. [Google Drive](https://drive.google.com) → **"내 드라이브"** 에서 그 폴더를 찾습니다.
2. 폴더 우클릭 → **"공유"** → 일반 액세스를 **"링크가 있는 모든 사용자"** + 권한은
   **"뷰어"** 로 변경 → **"링크 복사"**.
   (이 설정을 해야 사이트에 폴더 내용이 미리보기로 보입니다)

### 3) 특정 일정에 업로드 기능 켜고 링크 붙여넣기

- **편집기(editor.html)** 에서 "⑦ 시간별 일정" 패널 → 해당 일정 카드의
  **"📸 참가자에게 이 활동 사진 업로드 받기"** 체크박스를 켜세요.
- 나타나는 두 칸에 1)에서 복사한 **구글폼 링크**, 2)에서 복사한 **구글 드라이브
  폴더 링크**를 각각 붙여넣습니다. 바로 아래에 폴더 내용 미리보기가 뜹니다.
- 또는 `content.js`에서 해당 일정 항목의 `photoUpload.enabled`를 `true`로 하고
  `formUrl`, `driveFolderUrl` 값을 직접 채워도 됩니다.

이렇게 켜둔 활동은 참가자 사이트의 **"사진업로드"** 메뉴(하단 탭)에 자동으로
나타나고, 해당 일정 상세 화면에도 그 메뉴로 안내하는 버튼이 붙습니다.

### 참고

- 활동(회차)마다 별도의 폼/폴더를 만들어 서로 다른 일정에 연결할 수 있습니다.
- 구글 드라이브 저장 용량(개인 계정 기준 보통 15GB 무료)을 넘지 않는지만
  가끔 확인해주세요.
- 드라이브 폴더 미리보기는 지도 페이지의 구글맵 임베드와 같은 방식(API 키 불필요)
  이라, 폴더가 "링크가 있는 모든 사용자"로 공유되어 있어야만 정상적으로 보입니다.

---

## ✈️ 항공권 정보 기능 설정하기 (선택)

지방 출발 참가자를 위해 **항공편/예약번호를 참가자가 본인 성함으로 직접
조회**할 수 있게 해주는 기능입니다. 원래 업무 흐름(① 구글폼으로 항공 희망
정보를 받고 → ② 예약 완료 후 엑셀 명단을 공유)을 그대로 유지하면서,
②의 "엑셀 파일을 따로 공유"하는 부분만 사이트 안에서 조회하는 방식으로
바꿔주는 것입니다. 기본적으로 꺼져 있어서, 필요 없는 행사에서는 신경 쓰지
않아도 됩니다.

### 1) 편집기에서 섹션 켜기

**편집기(editor.html)** 의 **"✈️ 항공권 정보"** 패널에서 **"이 섹션 사용"**
체크박스를 켭니다.

### 2) 구글폼 링크 / 안내 문구 입력

기존에 쓰던 항공 희망 정보 수집용 구글폼 링크를 그대로 붙여넣고, 필요하면
참가자에게 보여줄 안내 문구를 한 줄 적습니다.

### 3) 엑셀 명단을 표로 붙여넣기

예약이 끝난 뒤 정리한 엑셀(성함 / 출발일자 / 출발항공사 / 출발시각 / 출발지 /
출발편명 / 출발예약번호 / 복귀일자 / 복귀항공사 / 복귀시각 / 복귀지 / 복귀편명 /
복귀예약번호 순서)에서 표 범위를 그대로 드래그해서 복사(Ctrl/Cmd+C)한 뒤,
**"엑셀에서 복사한 표 붙여넣기"** 칸에 붙여넣고 **"📋 붙여넣은 표에서
가져오기"** 를 누르면 명단이 자동으로 채워집니다. 맨 앞의 순번(NO.) 칸이나
머리글(제목) 줄이 같이 복사돼도 알아서 걸러집니다. 개별 항목은 아래 목록에서
직접 수정하거나 삭제할 수 있고, 다시 붙여넣으면 기존 명단 뒤에 **추가**되니
처음부터 다시 넣고 싶다면 먼저 "전체 삭제"를 눌러주세요.

이렇게 등록해두면 참가자 사이트의 **"항공권"** 메뉴(하단 탭)에서 본인
성함으로 검색해 자신의 출발편/복귀편/예약번호만 확인할 수 있습니다. (다른
사람의 예약번호는 검색 결과에 노출되지 않지만, 명단 데이터 자체는
`content.js` 파일 안에 그대로 들어있으니 — 이 사이트의 다른 데이터와
마찬가지로 — 완전히 비공개는 아니라는 점은 참고해주세요.)

---

## ✅ 행사 전 최종 체크리스트

- [ ] `content.js`의 `eventName`, `date`, `dateDisplay` 확인
- [ ] `index.html` `<head>`의 `og:title`/`og:description`/`og:image` 3줄을 `eventName`/`heroTagline`/`heroImage`와 같은 값으로 수정 (카카오톡 공유 미리보기용 — 자동 반영 안 됨)
- [ ] `notice.active`를 상황에 맞게 켜고(true) 문구 확인
- [ ] `contact` (담당자 이름/전화번호) 최신 정보로 수정
- [ ] `schedule` 배열의 시간/장소/지도 링크(`mapUrl`) 실제 주소로 확인
- [ ] `images/hero.jpg`, `staff-*.jpg` 사진 교체
- [ ] `survey.url` 설문 폼 링크 실제 주소로 교체
- [ ] 스마트폰 실기기에서 최종 확인 (전화 버튼, 지도 버튼 클릭 테스트)
